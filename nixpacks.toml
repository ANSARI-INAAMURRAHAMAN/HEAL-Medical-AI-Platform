[phases.setup]
nixPkgs = ["python39", "pip"]

[phases.install]
cmds = ["cd ml-diagnosis-tool-main && pip install -r requirements.txt"]

[phases.build]

[start]
cmd = "cd ml-diagnosis-tool-main && python -m gunicorn app:app --host=0.0.0.0 --port=$PORT"
